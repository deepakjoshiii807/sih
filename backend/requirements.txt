# Core
Django==4.2.16
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
# Bundles the IANA timezone database for Asia/Kolkata etc. on hosts without system tzdata
tzdata==2025.2

# Production
gunicorn==23.0.0            # WSGI server
psycopg[binary]==3.2.9      # PostgreSQL driver (Django >= 4.2 uses psycopg 3 automatically)
dj-database-url==2.3.0      # DATABASE_URL -> Django settings
whitenoise==6.9.0           # static files behind the WSGI server (admin assets)
